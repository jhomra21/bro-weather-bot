# For SMTP (worker-mailer), set these as secrets in production:
SMTP_HOST="smtp.example.com" 
SMTP_PORT="465" # 465 or 587
SMTP_USERNAME="user@example.com"
SMTP_PASSWORD="your-password"
SMTP_SECURE="true" # true for port 465
SMTP_STARTTLS="false" # true for port 587

# Optional: Used to generate absolute unsubscribe links in emails.
# Set this to your worker URL (custom domain or workers.dev):
BASE_URL="https://your-worker.example.workers.dev"